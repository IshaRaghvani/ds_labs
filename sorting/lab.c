// Using Quick sort, assign the roll nos. to the students of your class as per
// individual Searching their previous years result. i.e. topper will be roll no. 1.

#include <stdio.h>
#include <stdlib.h>
struct stud s
{   
    int result;
    int roll_no;
};

void quicksort(struct stud s[] , int high , int low){

    int i,j,pivot;
    
    if(i<j){
        pivot=low;
        i=low;
        j=high;
        while(i<j){
            while(s[i].result )
        }
    }
}
int main ()
{
    int n,cgpa;
    printf("enter number of student: ");
    scanf("%d", &n);
    for(int i=0;i<n;i++){

        printf("Enter previous cgpa: ");
        scanf("%d", &cgpa);
    

    }

   
}